<Window x:Class="Watics.Window1"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Watics"
        mc:Ignorable="d"
        Title="MainTitle" Height="480" Width="640">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0">
            <Image x:Name="titleImage" Source="title.jpg"/>
        </StackPanel>
        <DockPanel Grid.Row="1" Margin="10">
            <Button x:Name="Start" Content="start" FontSize="20" Click="Start_Click" DockPanel.Dock="Top"/>
            <Button x:Name="Load" Content="load" FontSize="20" Click="Load_Click" DockPanel.Dock="Top"/>
            <Button x:Name="Quit" Content="Exit" FontSize="20" Click="Quit_Click" DockPanel.Dock="Top"/>
            <Label Content="2019 윈도우즈프로그래밍 조기은 신준식 임성일" VerticalAlignment="Bottom"/>
        </DockPanel>
    </Grid>
</Window>
